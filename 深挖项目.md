# milvus
1. 两种索引类型 hnsw 内存占用高   ivf  其他效率应该差不多
2. hnsw是通过多层的导航图，类似于跳表，从最稀疏那一层寻找；ivf是通过kmeans聚类的算法  N个簇  通过query与中心点距离，逐一比对。
数据类为 Collection（集合）、Field（字段） 和 Schema（架构）
字段名称	类型	核心参数	业务用途
- pk	INT64	is_primary=True, auto_id=False	文档唯一标识（通常关联 Docstore ID）
- dense_vector	FLOAT_VECTOR	dim=1024 (取决于模型)	语义召回（处理模糊概念）
s- parse_vector	SPARSE_FLOAT_VECTOR	无需预设 dim	关键词召回（处理特定条款编号、费率）
- metadata	JSON	-	存储页码、章节、修订日期等溯源信息

# Ragas 和 DeepEval
## ragas
主要用于 RAG 指标评估，适合批量跑测试集。
常用输入包括：

- `question`：用户问题
- `contexts`：检索到的上下文
- `answer`：模型回答
- `citations`：引用来源
- `ground_truth`：标准答案，可选

Ragas 可以基于这些内容进行打分，常见指标包括：

- **Faithfulness**：回答是否忠于 `contexts`，是否存在编造
- **Answer Relevance**：回答是否真正回应了 `question`
- **Context Precision**：检索到的 `contexts` 是否相关，噪声是否过多
- **Context Recall**：标准答案所需的依据是否都包含在 `contexts` 中
- **Answer Correctness**：回答是否与 `ground_truth` 一致

## DeepEval

DeepEval 更像一个测试框架，适合写成自动化测试，并放入 CI 或回归测试流程。

例如，你有 1200 条测试用例。每次调整分块策略、Prompt、Reranker 或 Embedding 模型后，都可以重新跑一遍测试集。

简单来说：

- **Ragas** 负责“打分看质量”
- **DeepEval** 负责“把质量要求变成自动化测试门禁”

# Rerank
- 基于 Cross-Encoder 的点对点评分 (Pointwise)
这是最标准的策略。模型同时输入 Query 和 Document，输出一个 $0 \sim 1$ 之间的相关性分数。
- 基于大模型（LLM）的重排 (Listwise)
- 延迟交互模型 (Late Interaction / ColBERT)

金融数据具有极强的结构约束（如条款层级、数值精度）。单纯依靠开源重排模型往往不够，建议引入以下组合拳：
1. 规则加权 (Rule-based Boosting) 对于时效性、层级加权
2. 负面约束剔除 (Negative Constraint Filtering) 一票否决加权
3. 精排在 LangGraph 中的工程位置
# MCP
fastmcp
```python
import os
import sqlite3
from typing import List, Optional
from fastmcp import FastMCP

# 初始化 FastMCP 实例
# 默认支持 stdio 传输协议，适用于 Claude Desktop 或自定义 MCP Client
mcp = FastMCP("Heterogeneous-Data-Gateway")

# --- 场景 1: SQL 数据库集成 ---
DB_PATH = "finance_data.db"

def init_db():
    """初始化演示数据库"""
    with sqlite3.connect(DB_PATH) as conn:
        conn.execute("CREATE TABLE IF NOT EXISTS compliance_logs (id INTEGER PRIMARY KEY, term TEXT, status TEXT)")
        conn.execute("INSERT OR IGNORE INTO compliance_logs (id, term, status) VALUES (1, 'RAG-Audit', 'Passed')")

@mcp.tool()
def query_compliance_db(sql_query: str) -> str:
    """
    执行 SQL 查询以获取金融合规性数据。
    参数:
        sql_query: 标准 SQLite 查询语句。
    """
    try:
        with sqlite3.connect(DB_PATH) as conn:
            cursor = conn.cursor()
            cursor.execute(sql_query)
            results = cursor.fetchall()
            return str(results)
    except Exception as e:
        return f"Database Error: {str(e)}"

# --- 资源暴露 (Resources) ---
@mcp.resource("config://app-settings")
def get_config() -> str:
    """暴露静态配置文件作为资源"""
    return "Mode: Production; Access-Level: High-Audit"

if __name__ == "__main__":
    init_db()
    # 启动 MCP Server（默认使用 stdio 协议）
    mcp.run()
```

# 大模型微调
LoRA (Low-Rank Adaptation) 及其变体
1. 训练参数量仅为全量的 $0.1\% \sim 1\%$，但在特定任务上的表现可与全参数微调持平
2. 通过JSONL的格式 设置对话集（）和指令式（query、output、input）
- 秩（Rank, $r$）：这是最核心的超参数。关系：$r$ 越大，可训练参数越多，表达能力越强，但显存占用和过拟合风险也随之增加。对于金融合规这类逻辑严密的任务，通常 $r=16$ 或 $r=32$ 已足够。
- 缩放系数（Alpha, $\alpha$）：关系：通常计算为 $\frac{\alpha}{r} \cdot \Delta W$。$\alpha$ 类似于学习率的缩放器，用于调节旁路权重对主路径的影响程度。
- 目标层（Target Modules）：通常作用于 Transformer 中的 Attention 层（Query/Value 矩阵）。研究表明，同时微调 $Q$ 和 $V$ 矩阵通常比只微调单一矩阵效果更好。

# 基金合同合规知识库系统（RAG 大模型）面试深挖问答

## 1. 如何评估这个 Agent 的性能？

可以从五层评估：

- **检索质量**：Hit Rate@K、Recall@K、MRR、NDCG，关注正确证据是否被召回。
- **生成质量**：Faithfulness、Answer Correctness、Citation Accuracy、Hallucination Rate。
- **Agent 决策能力**：是否正确触发 Rewrite、Step-Back、HyDE、二次检索。
- **业务风险指标**：人工复核通过率、幻觉率、高风险错误率、引用完整率。
- **线上可观测性**：记录 query、召回 chunk、rerank 分数、Agent 路由、最终引用。

面试总结：

> 我会把 Agent 性能拆成检索召回、答案忠实性、Agent 路由决策、业务风险和线上可观测性五类指标。离线用 Golden Dataset + Ragas/自研评测做回归测试，线上通过全链路 trace、用户反馈和人工复核监控幻觉率、引用准确率和高风险错误率。

## 2. 检索层评估和 Agent 决策评估有什么区别？

**检索层评估**关注：

> 正确证据有没有被找回来？

例如 Hit Rate@10、Recall@10、MRR。

**Agent 决策评估**关注：

> 如果首轮检索不理想，Agent 是否会正确重写、扩展、重试或拒答？

例如是否正确触发 HyDE、Step-Back、Query Rewrite，是否避免无效循环。

简单说：

```text
检索层：评估证据召回能力
Agent 层：评估策略选择和补救能力
```

## 3. 没有大规模人工 Ground Truth，1200 个测试用例怎么构建？

不是纯人工写，也不是完全相信 LLM，而是：

```text
真实文档结构抽样 + LLM 受控生成 + 自动校验 + 少量人工抽检
```

流程：

1. 从基金合同、招募说明书、法规中抽取章节、条款、表格、定义项。
2. 每条样本绑定 `doc_id`、`chunk_id`、章节号、条款号、原文 span。
3. LLM 只基于给定 evidence unit 生成问题、答案草稿、引用依据。
4. LLM-as-a-Judge 检查答案是否被原文支持、引用是否正确、问题是否可答。
5. 用规则校验结构字段，用反向检索验证问题能否找回原始证据。
6. 高风险样本人工抽检。

测试集分层：

```text
Gold Set：人工确认，用于核心回归
Silver Set：LLM 合成 + 自动校验，用于大规模覆盖
Hard Set：多跳、表格、拒答、冲突类困难样本
```

## 4. 300/1200 字三级分块中，“自动向上合并至父块”怎么触发？

策略是：

```text
叶子块负责精确召回，父块负责上下文补全
```

不是一开始就直接检索父块，而是先检索 300 字叶子块，再根据命中情况动态扩展到 1200 字父块。

触发条件：

- **高置信单点命中**：top1 分数高，并且父块是完整条款。
- **同父块多子块聚集**：TopK 中多个叶子块来自同一父块。
- **结构完整性触发**：命中表格、长列表、费用条款、风险揭示等。
- **语义不完整触发**：叶子块无法覆盖前置条件、例外条款或脚注。

父块得分可类似：

```text
parent_score = max(child_score)
             + α * sum(top_child_scores)
             + β * child_hit_count
             + structure_boost
```

## 5. 分块方案如何设计 Schema？

核心设计：

```text
documents：文档级元数据
chunks：section / parent / leaf 三级块
chunk_edges：父子层级关系
chunk_tables / table_cells：表格结构
chunk_lists / list_items：长列表结构
chunk_embeddings：向量索引元数据
answer_citations：答案引用与溯源
```

关键点：

- Milvus 只索引 `leaf chunk`，降低向量存储压力。
- `parent_chunk_id` 用于检索后向上扩展。
- 表格不简单拍平成文本，而是保存表头、行列、脚注。
- 每个 chunk 带章节号、条款号、版本、文档类型、offset，支持确定性溯源。

检索链路：

```text
leaf 检索 -> RRF 融合 -> parent 聚合 -> 动态扩展父块/表格/列表 -> 生成答案
```

## 6. Dense 和 BM25 结果冲突时，如何保证确定性溯源？

冲突例子：

```text
Dense：语义相似，但条款编号错误
BM25：编号精确，但命中目录或相邻上下文
```

处理原则：

```text
RRF 负责候选融合，不能直接决定最终引用
最终溯源由 metadata 校验 + reranker + citation validation 决定
```

具体策略：

- RRF 保留 dense、BM25、父子检索多路候选。
- 对条款号、章节号、基金代码、日期做结构化解析。
- 编号类问题进入 precision profile，BM25 和 metadata 优先。
- Reranker 检查条款编号、文档类型、基金产品、正文位置、证据完整性。
- 最终答案只能引用通过校验的 evidence set。

## 7. 模糊概念查询时，混合检索权重如何分配？

精确查询：

```text
BM25 / metadata 更高
Dense 作为补充
```

例如：

```text
第六章第十八条
某个费率
某个日期
基金代码
```

模糊概念查询：

```text
Dense 更高
BM25 提供关键词锚点
必要时触发 Step-Back 或 HyDE
```

例如：

```text
大额赎回 -> 可能对应“巨额赎回”
暂停交易 -> 可能对应“暂停申购或赎回”
```

如果用 RRF，一般不直接做分数加权，而是通过：

```text
各路 topK 大小
rank boost
metadata filter
reranker feature
```

体现动态权重。

## 8. RRF 中常量 k 对条款编号召回有什么影响？

RRF 公式：

```text
score(d) = Σ 1 / (k + rank_i(d))
```

`k` 越大：

- rank1 和 rank10 差距变小。
- 融合更平滑。
- 但 BM25 精确编号命中可能被 dense 的语义相似结果稀释。

`k` 越小：

- Top1 优势更强。
- 有利于突出精确命中。
- 但会放大单路噪声，例如目录、页眉、相邻条款误命中。

金融场景中，不应只靠调 k。更稳的做法：

```text
k 保持中等偏稳，例如 50-60
编号类 query 使用 metadata filter、BM25 exact boost、正文块优先、reranker 编号一致性校验
```

## 9. LangGraph 中 Grading 节点的阈值逻辑如何定义？

Grading 不只依赖 LLM，而是组合判断：

```text
检索分数 + Reranker 分数 + 结构化字段校验 + LLM 证据充分性判断
```

常见阈值：

```text
高置信：
top rerank_score >= 0.75
evidence_coverage >= 0.8
LLM relevance = relevant
无 missing_evidence
=> Generate

中置信：
rerank_score 0.55 - 0.75
或 partially_relevant
=> 扩展父块 / 补检索 / Rewrite

低置信：
top rerank_score < 0.55
或 irrelevant
或 missing_evidence 不为空
=> Rewrite
```

对精确条款类问题，章节号、条款号、基金代码、文档类型必须匹配。

## 10. Rewrite 次数上限是多少？

通常设置：

```text
MAX_REWRITE = 2
```

也就是：

```text
第 0 次：原始 query 检索
第 1 次：rewrite 后重新检索
第 2 次：再次 rewrite 后重新检索
仍不通过：fallback / 拒答 / 人工复核
```

原因：

- 防止 query drift，问题被越改越偏。
- 控制延迟和成本。
- 避免模型在证据不足时强行生成。

## 11. HyDE 如何避免污染时效性金融检索？

核心原则：

```text
HyDE 只能扩展检索意图，不能生成事实依据
```

对时效性问题，例如：

```text
最近债市波动
当前收益率
最新监管新规
央行操作
具体日期区间
```

系统会标记为 `time_sensitive`，禁用事实型 HyDE。

允许 HyDE 输出：

```text
同义词
上位概念
相关制度名称
可能文档类型
检索关键词
```

禁止 HyDE 输出：

```text
具体收益率
市场涨跌结论
新规事实
日期判断
```

最终答案只能引用真实来源，例如基金合同、法规、公告、行情 API。HyDE 产物不能进入 citation。

## 12. Ragas 中 Faithfulness 高但 Answer Relevance 低，先调检索还是 Prompt？

优先调整 **Context 密度和证据选择**。

原因：

```text
Faithfulness 高：模型基本忠于上下文
Answer Relevance 低：回答没有真正切中问题
```

这通常说明上下文不够准，或者噪声太多。

金融合规场景中，“一字之差可能违规”，不能依赖 Prompt 让模型从噪声证据里自行筛选。

优先调整：

- 收紧 metadata filter。
- 提高 reranker 阈值。
- 减少无关父块扩展。
- 校验章节号、条款号、产品、版本。
- 提升 Context Precision。
- 对表格、长列表做结构化裁剪。

只有当证据已经准确但表达仍偏题时，才优化 Prompt 推理指令。

## 13. RAG 系统如何处理实时性更新？

不是 CDC 和定时任务二选一，而是按数据源区分：

```text
内部合同/公告：事件驱动或 CDC 增量索引
外部法规/交易所公告：定时拉取 + diff
债市行情/收益率：实时 API 或短周期缓存
```

内部文档更新流程：

```text
document_updated 事件
解析文档
结构化抽取章节/表格
重新分块
通过 chunk_hash 判断变化
只更新变化 chunk 的 embedding
更新 BM25 和 Milvus
旧版本标记 inactive
```

债市行情不建议全部向量化，应通过实时 API 查询后作为 structured context 注入。

## 14. 法规冲突或废止时，如何避免 LLM 引用旧规？

核心不是删除旧文档，而是做多版本和效力状态管理。

每个文档或 chunk 带：

```text
doc_version
publish_date
effective_date
expire_date
status: active / superseded / expired / revoked / draft
superseded_by
source_priority
```

默认检索只召回：

```text
status = active
effective_date <= query_time
expire_date 为空或 > query_time
```

只有用户明确问历史版本，才切换 historical profile。

Recency Weighting 只能作为排序辅助，不能替代法规效力判断：

```text
新发布 != 已生效
旧文件 != 一定无效
```

最终生成前做 citation validation，确保引用未废止、未被覆盖、版本与查询时间匹配。

## 15. 简历项目表述可优化为

> 设计三级层次化分块策略，采用 300 字叶子块进行精确检索，并保留 15%-20% 语义重叠以缓解分段语义断层；检索命中后根据子块评分分布、结构标签和 token 预算动态向上扩展至 1200 字父块。针对长列表、表格和费用条款，引入布局感知分块与结构化解析，保留表头、脚注和父子关系，在降低约 60% 向量存储压力的同时，提升合规依据召回完整性和确定性溯源能力。

## 16. 高频面试总结句

> 在金融合规 RAG 中，我不会让 embedding 相似度直接决定最终答案。检索层负责尽可能召回候选，RRF 负责融合多路结果，Reranker 和结构化字段校验负责确定最终证据，生成层只能基于通过校验的 evidence 回答。对于过期法规、错误版本、条款编号不一致或证据不足的场景，系统宁愿拒答或转人工复核，也不强行生成。
