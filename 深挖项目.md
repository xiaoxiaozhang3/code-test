### 你在项目中采用了 300/1200 字的三级分块及 15% - 20% 的语义重叠。

深挖点： 请详细说明“自动向上合并至父块”的触发逻辑是什么？是在检索阶段直接召回父块，还是根据子块的评分分布动态决定？

我们不是直接检索父块，而是先检索 300 字左右的叶子块，用细粒度 chunk 保证召回精度；然后在上下文构建阶段，
根据子块的分数、同父块聚集程度、结构标签以及 token 预算，动态决定是否向上合并到 1200 字父块。
比如 Top K 中多个子块集中命中同一个父块，或者命中的是表格、长列表、费用条款这类需要完整上下文的结构，就会返回父块；
如果只是一个定义类短答案，则保留叶子块，避免引入噪声。这样兼顾了精确召回和合规依据完整性。
  
核心挑战： 针对金融文档中的长列表和嵌套表格，你的“布局感知”具体是如何实现的（例如是基于 Markdown 转换、OCR 坐标还是 HTML 标签）？
为什么这种策略能有效降低 60% 的向量存储压力，这是否意味着你舍弃了部分冗余索引，又是如何保证不丢失合规关键细节的？

Schema 上我会把文档、分块、层级关系、布局结构和向量索引拆开设计。documents 保存文档级元数据；
chunks 统一保存 section、parent、leaf 三级块，并通过 parent_chunk_id 或 chunk_edges 表维护层级关系；
Milvus 只索引 leaf chunk，从而降低向量存储压力。对于表格和长列表，不直接拍平成普通文本，而是用 table_cells、list_items 
保存结构信息。检索时先召回 leaf，再根据 parent_chunk_id 动态扩展到 1200 字父块；如果命中 table/list，
则加载完整表头、脚注或列表上下文，保证生成答案时引用完整、可溯源。

### 混合检索与 RRF 融合的参数调优
RRF 里的 k 决定了不同排名之间的分数衰减速度。k 越大，rank1 和 rank10 的差距越小，融合结果更平滑，但在条款编号类问题上，
BM25 精确命中的优势可能被 dense 的语义相似结果稀释；k 越小，Top1 优势更强，有利于突出精确命中，但也会放大单路检索噪声，比如目录、页眉或相邻条款的误命中。
我们在金融场景里不会单纯依赖调 k，一般保持 k 在 50-60 的稳定区间，然后对编号类 query 通过 metadata filter、BM25 exact boost、正文块优先和 reranker 
编号一致性校验来保证确定性溯源。RRF 负责候选融合，最终引用必须通过结构化字段校验。










